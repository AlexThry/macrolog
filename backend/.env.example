# Copie ce fichier en .env et adapte les valeurs
PORT=3000

# IMPORTANT: génère une chaîne longue et aléatoire (ex: openssl rand -hex 32)
JWT_SECRET=remplace-moi-par-une-chaine-aleatoire-longue

# 'production' active le cookie secure (HTTPS requis). Laisse vide en local.
NODE_ENV=

# Chemin de la base (optionnel)
# DB_PATH=/data/macrolog.db
